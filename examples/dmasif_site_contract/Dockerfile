# Auto-generated by Lazarus for capability: dmasif_site
# Source: https://github.com/casperg92/MaSIF_colab (site weights + single-PDB path); https://github.com/FreyrS/dMaSIF (original) @ MaSIF_colab @ dMaSIF_Colab_V1; FreyrS/dMaSIF main
FROM lazarus/dmasif:site-ready

# Patches Lazarus applied during resurrection:
#   - geometry_processing.py: replaced `torch.FloatTensor([...], device=x.device)` in soft_distances() with `torch.tensor([...], dtype=torch.float, device=x.device)` so surface generation runs on CUDA (the legacy FloatTensor constructor rejects a CUDA device; the Colab worked around this by forcing CPU).
#   - Environment: substituted the task's KeOps python_engine branch (which imports cppyy; no cppyy wheel exists for this glibc-2.27 host and cppyy-cling fails to build) with released pykeops/keopscore 2.1.2 — same runtime NVRTC-JIT design, and the LazyTensor API dMaSIF uses is unchanged.
#   - Dependency pins for torch-1.8.1 era: numpy==1.23.5 (avoid np.float removal), googledrivedownloader==0.4 (module name google_drive_downloader expected by torch-geometric 1.6.1), plus plyfile/pyvtk needed at import time.

WORKDIR /work
