# Auto-generated by Lazarus for capability: diffdock_blind_docking
# Source: https://github.com/gcorso/DiffDock 
FROM lazarus/diffdock:site-ready

# Patches Lazarus applied during resurrection:
#   - Added lazarus_prep.py: fetches an RCSB PDB, writes an ATOM-only receptor PDB, extracts the crystal ligand instance and assigns correct bond orders from the CCD ideal-SDF template (RDKit AssignBondOrdersFromTemplate) to produce a ground-truth ligand SDF.
#   - Added lazarus_rmsd.py / lazarus_eval_batch.py: compute symmetry-corrected top-1 heavy-atom RMSD vs the crystal ligand via the repo's own get_symmetry_rmsd, using its two-molecule graph-isomorphism form so SMILES-derived predicted poses and crystal ligands need not share atom ordering.
#   - Added lazarus_smoke.py: self-contained TRUTHFUL sanity check — docks 6MOA/JW4 and asserts top-1 RMSD < 2.0 A (exit 0 iff BELOW 2.0), replacing the previous meaningless inverted 'RMSD >= 2.0' check on the hard 1a0q example.
#   - No source patches to DiffDock itself were needed; inference stack was already revived in the base image.

WORKDIR /work
