# Auto-generated by Lazarus for capability: sequoya_nsgaii_msa
# Source: https://github.com/benhid/Sequoya @ HEAD (shallow clone, 2020-era master)
FROM lazarus/sequoya:nsgaii-ready

# Patches Lazarus applied during resurrection:
#   - Installed genuine jmetalpy 1.5.3 from the jMetal/jMetalPy git tag v1.5.3 instead of the PyPI jmetalpy==1.5.4 wheel: the current PyPI 1.5.4 artifact is mislabeled and actually ships 1.6.0-era code (Problem became an ABC with number_of_objectives() as abstract methods and Requires-Python>=3.7), which is incompatible with Sequoya's attribute-based Problem API on Python 3.6. v1.5.3 restores the old attribute API Sequoya was written against.
#   - Pinned heavy deps to Python-3.6 wheels (numpy==1.19.5, scipy==1.5.4, pandas==1.1.5, matplotlib==3.3.4, statsmodels==0.12.2, plotly==4.14.3) with --only-binary=:all: to stop pip from resolving scipy to a 30MB source tarball (scipy 1.18) that cannot build on 3.6.
#   - Added examples/nsgaii_bb11001.py adapted from examples/nsgaii.py: BAliBASE(instance='BB11001', path='resources'); removed VisualizerObserver/ProgressBarObserver and all Plot/MSAPlot GUI calls; reduced max_evaluations to 3000; prints initial precomputed-population best SumOfPairs and final Pareto-front objectives + delta.
#   - Fixed jmetalpy API drift: StoppingByEvaluations takes max as a positional/`max=` arg in 1.5.3 (not max_evaluations=).
#   - pip install --no-deps -e . so `sequoya` is importable when running a script from examples/ (script dir, not repo root, is on sys.path).

WORKDIR /work
