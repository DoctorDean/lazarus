# Auto-generated by Lazarus for capability: despasito_saft_gamma_mie_propane_saturation
# Source: https://github.com/Santiso-Group/despasito @ b54781980e38bc16f82706ab30a29c51ed67c468
FROM lazarus/despasito:propane-sat-working

# Patches Lazarus applied during resurrection:
#   - Created despasito/_version.py (versioningit write-file was skipped under --no-build-isolation) with __version__='1.0.0' so `import despasito` succeeds.
#   - Added density_opts={pressure_min: 1e-4} to input_propane_sat.json so the low-temperature 125 K point (Psat~8 Pa, below the default 100 Pa pressure floor) resolves to a finite Psat/rhol/rhov; the 100 K near-zero point (exp rhov=0) remains numerically unresolvable and is the point the sanity spec skips. Higher-T points and the AARD are unchanged.
#   - Added lazarus_smoke.py smoke-check script to the example dir.

WORKDIR /work
