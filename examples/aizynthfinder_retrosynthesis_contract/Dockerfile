# Auto-generated by Lazarus for capability: aizynthfinder_retrosynthesis
# Source: https://github.com/MolecularAI/aizynthfinder @ v4.4.1
FROM lazarus/aizynth:working

# Patches Lazarus applied during resurrection:
#   - No source patches required. Two operational fixes: (1) created a conda env with Python 3.11 because the base image ships Python 3.14 which is outside the repo's supported range (>=3.10,<3.13); (2) `mkdir -p data` before `download_public_data data`, because the helper writes files into the target folder but does not create it, otherwise raising FileNotFoundError.

WORKDIR /work
