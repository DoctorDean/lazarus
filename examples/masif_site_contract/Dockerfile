# Auto-generated by Lazarus for capability: masif_site
# Source: https://github.com/LPDI-EPFL/masif @ MaSIF-site inference: prepare one PDB structure, run pretrained all_feat_3l model, output per-vertex interaction-site scores + ROC-AUC vs true interface. Verified ROC-AUC=0.9137 on 4ZQK_A.
FROM lazarus/masif:site-ready

# Patches Lazarus applied during resurrection:
#   - No source patches were required — the shipped environment already had msms, reduce, apbs, pdb2pqr, multivalue on PATH with MSMS_BIN/PDB2PQR_BIN/APBS_BIN/MULTIVALUE_BIN set, TensorFlow 1.12 importing fine (host has AVX), and pretrained weights present. The only broken piece is the built-in PDB download; bypassed by fetching 4ZQK.pdb from RCSB (https://files.rcsb.org/download/4ZQK.pdb) and feeding it through data_prepare_one.sh's --file mechanism.

WORKDIR /work
