# Auto-generated by Lazarus for capability: equidock_rigid_docking
# Source: https://github.com/octavian-ganea/equidock_public @ HEAD (--depth 1 clone of master, 2026-07-13)
FROM lazarus/equidock:working

# Patches Lazarus applied during resurrection:
#   - src/inference_rigid.py: dataset 'dips' -> 'db5' (match shipped db5 reference outputs + db5 checkpoint)
#   - src/inference_rigid.py: removed hardcoded personal debug paths input_dir/ground_truth_dir/output_dir = './test_sets_pdb/jean/' and 'jean_out', reverting to the real DB5 test directories
#   - src/inference_rigid.py: uncommented create_dir(output_dir) so the output directory is created
#   - src/inference_rigid.py: added optional EQUIDOCK_TARGET env-var filter to run inference on a single target (1AVX)

WORKDIR /work
