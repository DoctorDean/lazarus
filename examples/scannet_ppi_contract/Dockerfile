# Auto-generated by Lazarus for capability: scannet_ppi_binding_sites
# Source: https://github.com/jertubiana/ScanNet @ true
FROM lazarus/scannet:ppi-noMSA-proven

# Patches Lazarus applied during resurrection:
#   - utilities/paths.py: set library_folder = '/ScanNet/' so structures_folder/predictions_folder/model_folder/MSA_folder resolve to absolute in-repo paths (was empty '').

WORKDIR /work
