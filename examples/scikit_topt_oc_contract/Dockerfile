# Auto-generated by Lazarus for capability: scikit_topt_oc
# Source: https://github.com/kevin-tofu/scikit-topt 
FROM lazarus/scikit-topt:oc-working

# Patches Lazarus applied during resurrection:
#   - Added run_revival.py: a headless driver mirroring examples/tutorial/box_oc.py with export_img=False (no GL/VTK display), mesh_size coarsened 0.2->0.4, max_iters lowered 60->25, and a sanity assertion that reads the recorded compliance history from histories.npz.
#   - No source patches to scikit-topt were required; the installed 0.3.10 wheel runs as-is on x86-64 CPU with MPLBACKEND=Agg. PETSc/petsc4up not used.

WORKDIR /work
