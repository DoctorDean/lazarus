# Auto-generated by Lazarus for capability: ssalib_sst_ssa
# Source: https://github.com/ADSCIAN/ssalib @ 5f4d28998309410d644bcb455a99b2c67484a58a
FROM lazarus/ssalib:ssa-ready

# Patches Lazarus applied during resurrection:
#   - Added run_ssa.py runner: loads oisst-format SST (built-in load_sst() or an oisst-format JSON via $INPUT), runs SingularSpectrumAnalysis.decompose(), verifies SSA exactness by summing all elementary components vs the preprocessed series, reconstructs trend=[0,1]/seasonality=[2,3], exports to_frame() CSV + decomposition PNG + metrics.json. No source changes needed; package installs and runs as-is on Python 3.11 with numpy 2.4 / pandas 3.0 / scipy 1.15.

WORKDIR /work
