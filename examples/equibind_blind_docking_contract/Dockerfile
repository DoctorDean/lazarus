# Auto-generated by Lazarus for capability: equibind_blind_docking
# Source: https://github.com/HannesStark/EquiBind @ 4cb1b4c562dae914780154518a6b915bb4cba658
FROM lazarus/equibind:contract-ready

# Patches Lazarus applied during resurrection:
#   - Built conda env from environment_cpuonly.yml (python 3.7, torch 1.10 cpu, dgl, rdkit, openbabel, biopython).
#   - Cleared the executable-stack ELF flag on torch/dgl shared objects (patchelf --clear-execstack) to work around the hardened-kernel 'cannot enable executable stack' import failure on libtorch_cpu.so.
#   - Replaced the too-new solver-resolved dgl 2.0.0 (requires PyTorch>=1.13) with the torch-1.10-compatible dgl==0.9.0 CPU wheel from data.dgl.ai.
#   - Pinned pyaml==21.10.1 (the solver pulled pyaml 23.x which uses the 3.8 walrus operator and fails to import under Python 3.7).
#   - Added lazarus_dock.sh entrypoint that splits a complex PDB into receptor.pdb + crystal-ligand .sdf (via OpenBabel, adding hydrogens) and runs configs_clean/inference.yml, exporting the docked pose.

WORKDIR /work
